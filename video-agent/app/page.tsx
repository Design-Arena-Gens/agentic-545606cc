import { VideoAgent } from '@/components/video-agent';

export default function Home() {
  return (
    <main className="min-h-screen bg-gradient-to-b from-zinc-100 via-white to-white text-zinc-900">
      <VideoAgent />
    </main>
  );
}
